<?xml version="1.0" encoding="utf-8"?>
<!--The options menu for the app's toolbar-->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_home"
        android:title="@string/home"
        app:showAsAction="never" />

    <item android:id="@+id/action_explore"
        android:title="@string/explore"
        app:showAsAction="never"/>

    <item android:id="@+id/action_goals"
        android:title="@string/goals"
        app:showAsAction="never" />

    <item android:id="@+id/action_reset_profile"
        android:title="@string/reset_profile"
        app:showAsAction="never" />

</menu>